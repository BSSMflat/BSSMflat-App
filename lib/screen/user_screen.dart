import 'package:flutter/material.dart';

class userScreen extends StatelessWidget {
  const userScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("유저 스크린입니다"),
    );
  }
}