import 'package:flutter/material.dart';

class studyPaperScreen extends StatelessWidget {
  const studyPaperScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("학습지 스크린입니다"),
    );
  }
}